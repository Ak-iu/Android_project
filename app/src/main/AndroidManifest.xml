<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_project" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_steamtrack"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_steamtrack_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_project" >
        <activity
            android:name=".MostPlayedGamesActivity"
            android:exported="true" />
        <activity
            android:name=".FavActivity"
            android:exported="true"
            android:label="@string/title_activity_fav"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Android_project" />
        <activity
            android:name=".DetailsActivity"
            android:exported="true"
            android:label="@string/title_activity_details"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Android_project" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustNothing" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>